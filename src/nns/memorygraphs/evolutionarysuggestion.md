# 激活函数

不同模态相应峰值位置错开
根据模态需要，在CNN的之后一层处加sigmoid或开关函数

# GmemII结构 review时大量特征输入

转化为线性并检索/转化为卷积 ?

**算法探讨**

当前的semantic引导视线的方法在学习具有复杂边缘的大块连续二维特征时并不好用，视线在这块区域内打转

视线在看到这种大块连续二维特征时，期望模型能够学到：复杂边缘和内部颜色，应当考虑：当前图结构中特征组合方式是否需要变动、如何引导视线

（1）图结构特征表征调整：
指向节点本身的环形边？

（2）引导视线：
现在的IoR为边缘压制或墨西哥帽形，应当将MICRO和MACRO分开/考虑在多个GIInode切换时也切换IoR压制方式