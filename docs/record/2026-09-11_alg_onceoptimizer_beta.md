# 一次性区域构图器设计

提交者：chatgpt。记录时间：2026-09-11 15:08:41 CST。

## 更改概括

按用户要求，在 `docs/algorithm/Alg_onceoptimizer_beta.md` 中记录替换现有控制器与优化器的设计草案。本次仅修改文档，未实现算法、修改 CNN/Gmem/Gpos 或运行调参实验。

## 主要内容

- 评价以固定 CNN 特征分区、采样和一次性写图验证层级记忆的可行性。
- 用同模态局部亲和连接统一一维切向连续和二维面连续，补充种子、受约束区域合并、面域漂移、曲线分支及重叠处理。
- 定义实际采样锚点、几何覆盖、稀疏接触和采样预算。
- 明确现有边端点属于 GmemI、边存储于 GmemII 的接口事实；采用同模态区域星型及独立接触容器，不新增 Gmem 数据结构。
- 以按采样实例注册节点作为首版几何验证基线，将原型复用作为压缩对照，说明其内存代价与现有表示限制。
- 区分一次构图完成、语义识别完成及 Gpos 检索能力；给出模块划分、复杂度、只读查询及分阶段验证计划。

## 检查

核对了当前节点、关系写入和 Gpos 检索接口；检查新增文档的本地链接、Markdown 代码围栏及公式分隔符。本次没有运行算法实验。
