# 一次性构图 Notebook 图注与图例

提交者：chatgpt。记录时间：2026-09-11 21:26:05 CST。

## 更改内容

在 `src/test_memorypool_onceoptimizer.ipynb` 的输入图、支持与分割、单区域、星型与接触、查询响应等绘图单元中补充图例、图下注释和数值色条，并在相应 Markdown 单元添加中文读图表。

- 标明初始种子、事件候选、区域锚点、采样点、区域覆盖、星型边及接触边的含义。
- 区分可写门控、分割支持、区域标签、GposI 响应和参考结构评分；注明区域配色可重复、不表示语义类别。
- 明确查询图的青色叉号是期望锚点真值，不是预测峰。
- 二值图固定显示范围为 0～1，避免全白掩码因自动缩放显示成黑色。
- 图内图例采用英文，中文解释放在 Markdown 中，避免依赖额外中文字体。保留用户输入配置与已有运行输出；重新执行绘图单元才更新图片。

## 检查

Notebook JSON 及全部 10 个代码单元通过项目 Python 3.9 编译检查。未重新运行构图、检索或调参。
