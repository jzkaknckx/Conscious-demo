# 多图片顺序学习与内存资源诊断

修改 `src/test_memorypool_onceoptimizer.ipynb` 第 8 节，使用独立长期记忆测试 ILSVRC2014_train_0006 目录。只读确认该目录存在 659 张图片；没有执行 CNN、学习、检索或调参。本次不修改优化器及算法文档。

- 固定随机种子按文件划分，默认 593 张训练、66 张留出；可用 MAX_IMAGES 限制规模，或将 HOLDOUT_FRACTION 设为 0 学习全部图片。保存文件清单、配置及实验设置。
- 逐图记录三层节点、II 层关系、III 层成员与布局关系、稳定实体、原始／独立证据、新增／复用／归并／截断和耗时。文件内容哈希作为 episode；首次同 episode 重复学习后检查每条证据的独立机会与支持账目。
- 冻结后复查最早的 8 张训练图，计算实体编号保持率；对留出图片报告激活、覆盖率、几何误差与检索预算，对已知平移报告同编号根的位置一致性。显示示例图与实体编号／分数。明确这些代理指标不等于语义识别准确率，也不代表带标注的定位正确率。
- 使用 Linux /proc 报告进程 RSS、生命周期高水位、系统可用内存，并用后台线程每 0.1 秒采样本次峰值；CUDA 模式报告 allocated/reserved、峰值和可用显存。遍历持久池估算 Python 对象和去重 Tensor 存储，说明与工作区／分配器及进程 RSS 的区别。
- 逐图加载并释放观察报告；只保存轻量指标，不累计全部 LearningResult。每图追加 metrics.jsonl，结束时写 summary.json 和学习／资源曲线；失败／中断保留日志并停止，不将部分运行标为完成。日志不是模型检查点。
- 修正第 10 节的旧假设：不再将另一训练图片的实体写入根坐标覆盖到当前演示查询图，移除对 learning_results 的依赖。

验证：Notebook JSON 解析，全部 13 个代码单元通过 Python 静态 compile，git diff --check 通过。没有运行 Notebook 单元；实际吞吐、峰值内存和检索可靠性等待用户执行实验。
