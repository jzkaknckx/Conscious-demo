# 2026-07-26 Alg_optimizer_beta Gpos 工作视窗与二维面域自指关系

- 更新 `docs/algorithm/Alg_optimizer_beta.md` 的动态视窗口设计，将固定圆形窗口修订为由 Gpos 响应域、当前 Gmem 节点和连通约束生成的不规则工作视窗。
- 比较直接使用 Gpos 不规则区域、Gpos mask 叠加圆形窗口与种子化 Gpos 响应域工作视窗三种方案，并选用带面积预算和阻断图的种子化生长方案。
- 将动态尺度从离散半径候选改为候选面积预算与等效半径，保留固定总注意力预算、最低写入尺度和细节压力机制。
- 修订二维 surface 的动态引导方式，使用差分高斯正壳层、锥形距离核和同类 Gpos 低响应区域引导视线走向面域外沿，而不是回到面域内部。
- 新增二维连续 surface 的面域外延自指关系，使用连续角度扇区、径向外延、边界状态和孔洞候选压缩大面积同类特征的关系写入。
